# FarRing
A fast and efficient lockless ring buffer that adopts a (tunable) spaced backoff mechansim that achieves high throughput for shared memory 
The `Test` directory has code that benchmarks the performance (throughput)